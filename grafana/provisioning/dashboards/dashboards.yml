apiVersion: 1

dashboards:
  - name: RedCybers
    orgId: 1
    folder: "RedCybers"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
